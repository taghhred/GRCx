# Active classifier (NEW package)

Canonical load path used by the AI service:

`models/classifier/`

Contents are the **v6 trained export** from this package:
- `model.safetensors` (trained weights)
- `config.json` (IAM id2label mapping)
- `tokenizer.json` / `tokenizer_config.json`

Source version inside this package: `versions/v6/classifier/`

Do not use `Imtithal_AI_v2.1` — it is obsolete.
